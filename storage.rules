rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if true; // Allow read for authenticated users
      allow write: if false; // Keep write access restricted for now
    }
  }
}